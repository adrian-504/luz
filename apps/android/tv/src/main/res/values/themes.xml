<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window theme only; all UI colors come from the Compose theme (DESIGN_SYSTEM.md §3.1). -->
    <style name="Theme.IptvPlayer" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/bg_base</item>
        <item name="android:colorBackground">@color/bg_base</item>
    </style>
</resources>
